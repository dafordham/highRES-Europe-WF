# --- Python / general ---
__pycache__/
*.py[cod]
*.ipynb_checkpoints/
.DS_Store

# --- Conda / virtual envs ---
env/
.venv/
venv/
*.lock

# --- Snakemake working dirs ---
.snakemake/
*.snakemake.log

# --- HighRES: large input data from Zenodo ---
shared_input/
geodata/
weatherdata/
resources.zip
weatherdata.zip
geodata.zip

# If you keep resources/ under version control, remove this line:
#resources/

# --- HighRES: generated work/results ---
work/
**/work/
*.gdx
*.lst
*.log
*.dd
*.parquet
*.db
*.db.compressed
intermediate_data/
*.csv
*.ods
*.xls
*.xlsx
*.txt

# --- OS / editor junk ---
Thumbs.db
.idea/
.vscode/
*.swp

# Run output dirs, named work_<machine>_<horizon>_<mode> (plus the plain work/
# rule above). Glob so renaming the scheme cannot silently un-ignore gigabytes.
work_*/
**/work_*/

# Assistant scratch and instruction files. Kept on the laptop but never
# version-controlled, so they do not propagate to fox or to the group's fork.
CLAUDE.md
.claude/
